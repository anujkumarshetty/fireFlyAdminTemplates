import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#000000' },
  secondary: { main: '#f50057' }
};
const themeName = 'San Marino Razzmatazz Ring-tailed Mongoose';

export default createMuiTheme({ palette, themeName });